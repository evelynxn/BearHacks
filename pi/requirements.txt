# Punchi — Edge Agent dependencies (Raspberry Pi Zero 2 W)

pyaudio>=0.2.14
requests>=2.31.0
websocket-client>=1.7.0
python-dotenv>=1.0.0
SpeechRecognition>=3.10.0
pygame>=2.5.0
faster-whisper>=1.0.0
numpy>=1.24.0

# System packages — install with apt (NOT pip), see README.md:
#   sudo apt install -y portaudio19-dev mpg123
