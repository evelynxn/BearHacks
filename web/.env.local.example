# Copy this file to web/.env.local and fill in the TODOs.
# .env.local is gitignored at the repo root; this template file is checked in.

APP_BASE_URL=http://localhost:3001
AUTH0_DOMAIN=dev-kknv7y8p76evx881.us.auth0.com
AUTH0_CLIENT_ID=T5kJKr6yejuzyTPwWStyhxLq364CIqz1

# TODO: paste from Auth0 dashboard → Applications → (your app) → Settings → Client Secret
AUTH0_CLIENT_SECRET=

# TODO: generate a 64-char hex string with: openssl rand -hex 32
AUTH0_SECRET=

# TODO: point this at the Phase 2 orchestrator once it's running
# (default: http://localhost:3000 in dev)
NEXT_PUBLIC_ORCHESTRATOR_URL=http://localhost:3000
