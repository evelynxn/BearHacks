/** @type {import('next').NextConfig} */
const nextConfig = {
  // TODO: set NEXT_PUBLIC_ORCHESTRATOR_URL once the Express backend is deployed,
  // and add rewrites here if we want /api/* on the web app to proxy to it.
};

export default nextConfig;
